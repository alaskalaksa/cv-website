import {React } from 'react';

import Header from '../components/Header';

const HomePage = ({data}) => {

  const [blogs] = [];

  return (
    <div>
      {/* Page Header */}
      <Header />

    </div>
  );
};
export default HomePage;
